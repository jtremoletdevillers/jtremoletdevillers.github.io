// Stub function to fetch grade data
function fetchGradeData() {
    console.log("fetchGradeData called");
    // This function will fetch data in a future assignment
}

// Stub function to populate the gradebook table
function populateGradebook() {
    console.log("populateGradebook called");
    // This function will populate the table in a future assignment
}

// Call the stub functions to show they are connected
fetchGradeData();
populateGradebook();
